// craco.config.js
module.exports = {
    webpack: {
      configure: (webpackConfig, { env, paths }) => {
        // Example: You can customize Webpack configuration here
        return webpackConfig;
      }
    }
  };
  